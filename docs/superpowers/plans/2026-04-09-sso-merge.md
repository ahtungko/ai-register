# SSO Merge Script Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a standalone CLI script that merges Grok SSO text files into one output file without deduplication.

**Architecture:** Put the merge logic in a small helper module and expose a script entry point under `scripts/`. Keep the logic file-system focused and covered by a temporary-directory unit test.

**Tech Stack:** Python, pathlib, argparse, unittest

---

### Task 1: Add failing merge test and implementation

**Files:**
- Create: `util/sso_merge.py`
- Create: `scripts/merge_sso.py`
- Create: `tests_test_sso_merge.py`

- [ ] **Step 1: Write the failing test**
- [ ] **Step 2: Run test to verify it fails**
- [ ] **Step 3: Write minimal implementation**
- [ ] **Step 4: Run tests to verify they pass**
